# Rendered screenshot — Step 1

![Rendered Colab step 1](./screenshot.png)

- **Viewport:** 1440 × 1056 CSS pixels
- **Stage:** Canvas and outside background
- **Visible result:** Establish the page canvas, embedded reset, local fonts, design tokens, and neutral outside background.
- **Final reference:** [`../preview.jpg`](../preview.jpg)

This capture comes from the corresponding verified numbered implementation (`../1/screenshot.png`). The Colab page preserves that stage's DOM structure and CSS rules while moving the stylesheet into the document's `<style>` element.
